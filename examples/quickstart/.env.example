# Copy to .env and fill in a real key. .env is gitignored; .env.example is safe to commit.
# Only the key matching your configured provider (provider.type in plune.yaml) is needed.
# The offline evals (--only=tag:offline) need NO key at all.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=