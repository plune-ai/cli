# Install the bundled CPU build of onnxruntime-node and skip the large CUDA EP download.
# The GPU/CUDA binding fails to load on machines without CUDA (e.g. CI runners and CPU-only
# hosts). npm never publishes .npmrc, so this only affects local + CI installs here.
onnxruntime-node-install=skip
